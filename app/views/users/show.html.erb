

<div class='container'>
<div class='holder'>

<h1>Name: <%= @user.name %></h1>
<br>
<p>Email: <%= @user.email %></p>

<p>Date of birth: <%= @user.date_of_birth %></p>
<p>Role: <%= @user.role %></p>







<% unless current_user == @user %>
        <%= link_to "Add Friend", friendships_path(:friend_id => @user), :method => :post %>
    <% end %>

    <h4>Friends</h4>
    <ul>
        <% @user.friends.each do |friend| %>
        <li>
          <%= friend.email %> 
          <% if current_user == @user %>
            <%= link_to "Remove", friendship_path(:id => friend), :method => :delete %>
          <% end %>
        </li>
      <% end %>
    </ul>

    <% if current_user == @user %>
        <h4>Friend Requests</h4>
        <ul>
            <% @user.requested_friendships.each do |request| %>
            <li>
              <%= request.email %> 
              <%= link_to "Accept", friendship_path(:id => request), :method => "put" %> 
              <%= link_to "Decline", friendship_path(:id => request), :method => :delete %>
            </li>
          <% end %>
        </ul>
    <% end %>










<%= button_to('Delete User', user_path(@user), method: :delete, data:{confirm: "Delete user? Are you sure?"}) %>
<%= link_to("Edit user", edit_user_path(@user)) %>
<br>
<h2>Profile Photo</h2>
<br>
<%#= link_to image_tag(@user.profile_image.thumb.url), @user.profile_image.original.url if @user.profile_image? %>

<%= link_to image_tag(@user.profile_image.thumb.url), @user.profile_image.original.url if @user.profile_image?%> 

<h2>User Timeline</h2>


<% @user.photos.each do |photo| %>

<%# if photo.album_id == @album.id %>
<%#= recipe.name %>
<%= link_to(photo.title, photo_path(photo)) %>&nbsp;&nbsp;
<%= link_to image_tag(photo.photo_image.thumb.url), photo.photo_image.original.url if photo.photo_image? == true %>   
<br>
<%# else %>
<%# end %>


<% end %>
<br>
<br>





</div>


</div>